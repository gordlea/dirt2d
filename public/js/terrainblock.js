var TerrainBlock = dejavu.Class.declare({
    $name: "TerrainBlock",
    x: null,
    y: null,

    initialize: function(x,y) {
       this.x = x;
       this.y = y;

    },

    getVerts: function(scale_x, scale_y) {

    }
})